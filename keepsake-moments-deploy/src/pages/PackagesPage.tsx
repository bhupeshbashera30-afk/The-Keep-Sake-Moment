import { InquiryForm } from '../components/InquiryForm'
import { SectionIntro } from '../components/SectionIntro'
import { packageOptions } from '../lib/data'
export function PackagesPage() { return (<div><section className="mx-auto max-w-7xl px-4 py-20 md:px-8"><SectionIntro eyebrow="Packages" title="Choose a package direction, then let the team personalise it." description="Packages act as premium starting points. Users can select the service they want, share the event basics, and the team will handle pricing refinement, scope, and execution details from there." /><div className="mt-12 grid gap-6 md:grid-cols-3">{packageOptions.map((option) => (<article key={option.name} className="rounded-[2rem] border border-burgundy-100 bg-white p-8 shadow-soft"><h3 className="font-serif text-3xl text-burgundy-950">{option.name}</h3><p className="mt-4 text-sm leading-7 text-burgundy-700">{option.description}</p><ul className="mt-5 space-y-2 text-sm text-burgundy-700">{option.includes.map((item) => (<li key={item}>• {item}</li>))}</ul></article>))}</div></section><section className="mx-auto max-w-7xl px-4 pb-20 md:px-8"><div className="grid gap-10 lg:grid-cols-[0.8fr_1.2fr]"><div><h2 className="font-serif text-4xl text-burgundy-950">Package enquiry</h2><p className="mt-4 text-sm leading-7 text-burgundy-700">Users do not need to finalize every detail on the site. The package form only needs to capture intent, timeline, and event context so the team can reach out and continue the process.</p></div><InquiryForm compact /></div></section></div>) }
